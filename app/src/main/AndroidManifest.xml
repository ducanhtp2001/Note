<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Note"
        tools:targetApi="31">
        <activity
            android:name=".uI.settings.ChangePassActivity"
            android:exported="false" />
        <activity
            android:name=".uI.School.show_class.ShowClassActivity"
            android:exported="false" />
        <activity
            android:name=".uI.home.edit_note.EditNoteActivity"
            android:theme="@style/AppTheme.EditNoteActivity">

            <!-- Other activity attributes -->
        </activity>
        <activity
            android:name=".activities.home.MainActivity"
            android:exported="false" />
        <activity
            android:name=".activities.forgot.ForgetPassActivity"
            android:exported="false" />
        <activity
            android:name=".activities.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Note.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".uI.home.add_note.AddNoteActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|locale"
            android:theme="@style/Theme.Note.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>